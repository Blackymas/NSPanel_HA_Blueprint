---
name: Build ESPHome

# yamllint disable-line rule:truthy
on:
  push:
    paths:
      - "nspanel_esphome*.yaml"
      - "esphome/nspanel_esphome*.yaml"
      - "prebuilt/nspanel_esphome*.yaml"
      - "prebuilt/wall_display*.yaml"
      - ".test/*.yaml"
      - "*.h"
      - "*.c"
      - "*.cpp"
      - "*.py"
  pull_request:
    paths:
      - "nspanel_esphome*.yaml"
      - "esphome/nspanel_esphome*.yaml"
      - "prebuilt/nspanel_esphome*.yaml"
      - "prebuilt/wall_display*.yaml"
      - ".test/*.yaml"
      - "*.h"
      - "*.c"
      - "*.cpp"
      - "*.py"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  setup_environment:
    name: Setup Environment & Cache Dependencies
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache ESP-IDF & PlatformIO dependencies
        id: cache-idf
        uses: actions/cache@v4
        with:
          path: |
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esp-idf-${{ hashFiles('**/esphome_idf_basic.yaml') }}
          restore-keys: |
            ${{ runner.os }}-esp-idf-

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Restore ESPHome cache
        id: cache-esphome
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome
          restore-keys: |
            ${{ runner.os }}-esphome-

      - name: Install pipx
        run: python -m pip install --upgrade pip pipx

      - name: Ensure pipx PATH
        run: echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Install ESPHome (if not cached)
        if: steps.cache-esphome.outputs.cache-hit != 'true'
        run: pipx install esphome

      - name: Verify ESPHome
        run: esphome version

      - name: Save ESPHome cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-esphome-

  code_scan:
    name: Code scan (YAML)
    runs-on: ubuntu-latest
    needs: setup_environment
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install yamllint
      - run: find . -name "*.yaml" -exec yamllint -c ./.rules/yamllint.yml {} +

  build_core:
    name: Core
    needs: [code_scan, setup_environment]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome
          restore-keys: |
            ${{ runner.os }}-esphome-
      - run: echo "$HOME/.local/bin" >> $GITHUB_PATH
      - run: esphome compile ".test/esphome_idf_basic.yaml"
      - name: Save updated ESPHome cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-esphome-

  build_core_arduino:
    name: Core (Arduino)
    needs: build_core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome
          restore-keys: |
            ${{ runner.os }}-esphome-
      - run: echo "$HOME/.local/bin" >> $GITHUB_PATH
      - run: esphome compile ".test/esphome_ard_basic.yaml"
      - name: Save updated ESPHome cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-esphome-

  build_ble_tracker:
    name: BLE Tracker
    needs: build_core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome
          restore-keys: |
            ${{ runner.os }}-esphome-
      - run: echo "$HOME/.local/bin" >> $GITHUB_PATH
      - run: esphome compile ".test/esphome_idf_ble_tracker.yaml"
      - name: Save updated ESPHome cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-esphome-

  build_bluetooth_proxy:
    name: Bluetooth Proxy
    needs: build_core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome
          restore-keys: |
            ${{ runner.os }}-esphome-
      - run: echo "$HOME/.local/bin" >> $GITHUB_PATH
      - run: esphome compile ".test/esphome_idf_bluetooth_proxy.yaml"
      - name: Save updated ESPHome cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-esphome-

  build_climate_cool:
    name: Climate Cool
    needs: build_core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome
          restore-keys: |
            ${{ runner.os }}-esphome-
      - run: echo "$HOME/.local/bin" >> $GITHUB_PATH
      - run: esphome compile ".test/esphome_idf_climate_cool.yaml"
      - name: Save updated ESPHome cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-esphome-

  build_climate_heat:
    name: Climate Heat
    needs: build_core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome
          restore-keys: |
            ${{ runner.os }}-esphome-
      - run: echo "$HOME/.local/bin" >> $GITHUB_PATH
      - run: esphome compile ".test/esphome_idf_climate_heat.yaml"
      - name: Save updated ESPHome cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-esphome-

  build_climate_dual:
    name: Climate Dual
    needs: build_core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome
          restore-keys: |
            ${{ runner.os }}-esphome-
      - run: echo "$HOME/.local/bin" >> $GITHUB_PATH
      - run: esphome compile ".test/esphome_idf_climate_dual.yaml"
      - name: Save updated ESPHome cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-esphome-

  build_cover:
    name: Cover
    needs: build_core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome
          restore-keys: |
            ${{ runner.os }}-esphome-
      - run: echo "$HOME/.local/bin" >> $GITHUB_PATH
      - run: esphome compile ".test/esphome_idf_cover.yaml"
      - name: Save updated ESPHome cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/pipx/venvs/esphome
            ~/.local/bin/esphome
            ~/.esphome/cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-esphome-${{ github.job }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-esphome-
...
