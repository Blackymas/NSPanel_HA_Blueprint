name: Code scan

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * *"

jobs:
  code_scan:
    name: Code scan
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v3
      - name: Validate YAML file
        run: yamllint nspanel_blueprint.yaml
